# Vibebox VM edition

The VM edition is a clean Ubuntu 24.04 guest managed from Windows through the
`vibebox.ps1` command. It is under construction until the phases in
`docs/vm-work-orders.md` reach Gate A.

This directory is intentionally independent of the frozen container edition.
Use the host prerequisites and phase runbooks under `vm/docs/` before creating
an instance.
