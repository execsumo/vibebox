<<DELEGATE:DONE>> I updated backup.sh, backup.ps1, restore.sh, and restore.ps1 to support backing up and conditionally restoring the ssh-keys volume. I did NOT modify scripts/backup because it runs inside the container as the unprivileged dev user and cannot genuinely read the root-owned SSH host keys in /opt/vibebox/ssh, so per instructions I left it alone. For DoD check 6, the restore scripts explicitly check if the ssh-identity/ directory exists in the tarball (using 'tar tf') and only mount the volume and attempt to wipe/restore it if it is present; if absent while the flag is passed, it degrades gracefully and prints a notice that the existing identity is kept.
